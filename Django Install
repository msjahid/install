sudo apt install virtualenv && sudo apt install python-django-common && sudo apt-get install python3-django && sudo apt-get install python3-pip && pip3 install django && sudo apt install python3-venv && pip3 install virtualenv && sudo pip3 install django && 

django-admin --version
2.1.2

If error: python -m pip install django

$$ mkdir django_project
$$ cd django_project
$$ . env/bin/activate
$$ django-admin startproject testsite
$$ python3 manage.py runserver

sudo apt-get install wget ca-certificates && wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add - && sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt/ `lsb_release -cs`-pgdg main" >> /etc/apt/sources.list.d/pgdg.list' && sudo apt-get update && sudo apt-get install postgresql postgresql-contrib && sudo apt-get install pgadmin4 pgadmin4-apache2


$$ sudo su -postgres
$$ psql 
$$ CREATE USER msjahid WITH SUPERUSER PASSWORD 'jahid111';



